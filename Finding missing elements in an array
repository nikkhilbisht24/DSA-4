#include <stdio.h>

int main()
{
    int arr[] = {0, 1, 2, 3, 4, 5, 7};
    int sum = 0, i, n = 7;
    for (i = 0; i < n; i++)
    {
        sum = sum + arr[i];
    }
    int s, missing;
    s = n * (n + 1) / 2;
    missing = s - sum;
    printf("%d", missing);

    return 0;
}
